name: semantic_ir_linter
description: Experimental semantic IR-based linter that mirrors Flutter's runtime semantics tree for high-confidence accessibility analysis.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^8.4.0
  collection: ^1.18.0
  custom_lint_builder: ^0.8.1
  meta: ^1.11.0

dev_dependencies:
  lints: ^3.0.0
  path: ^1.8.3
  test: ^1.26.3
